apply plugin: 'com.android.library'

apply from: '../../buildConfig/android.config.gradle'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation "com.google.android.material:material:$material_version"
}